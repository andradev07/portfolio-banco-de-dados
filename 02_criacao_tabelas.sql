CREATE TABLE informacoes_produtos ( 
id INT AUTO_INCREMENT PRIMARY KEY,  -- comando para o id ser gerado automaticamente conforme for inserido e definido como chave primária
nome VARCHAR(100),
descricao VARCHAR(100)
preco DECIMAL(10,2),
categoria VARCHAR(50)
);
